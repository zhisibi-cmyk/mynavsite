my-nav/
├── data/
│   └── db.json       # 自动生成的数据库文件
├── public/
│   └── index.html    # 前端页面（包含前台和后台逻辑）
├── package.json      # 项目依赖配置
├── server.js         # 后端核心代码
├── docker-compose.yml # Docker 部署配置
└── Dockerfile        # 镜像构建文件